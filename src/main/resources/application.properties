spring.application.name=ArtGalleryHub

# ----- Server -----
server.port=8080

# ==== H2 FILE (PROD-LIKE) ====
# ?? ??????????? ? ???? ./data/art_gallery_hub_prod.mv.db
spring.datasource.url=jdbc:h2:file:./data/art_gallery_hub_prod;DB_CLOSE_ON_EXIT=FALSE;AUTO_SERVER=TRUE
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver

## ----- PostgreSQL PROD -----
#spring.datasource.url=jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}
#spring.datasource.username=${DB_USERNAME}
#spring.datasource.password=${DB_PASSWORD}
#spring.datasource.driver-class-name=org.postgresql.Driver

# ------ JPA -------
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

#---- Liquibase -------
spring.liquibase.enabled=true
spring.liquibase.change-log=classpath:/db/changelog/db.changelog-master.xml

#---- Files uploading -------
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

upload.dir=./img/artworks
spring.web.resources.static-locations=file:${upload.dir}/
